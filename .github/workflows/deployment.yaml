name: Test CICD 
on:
  push:
    # Sequence of patterns matched against refs/heads
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: echo "Test Action trigger pipelines"